# EDA Findings

> Generated by `scripts/run_eda.py`. Re-run the script after updating the datasets or manifests.

## VQA-RAD (Evaluation Benchmark)

Computed over the official 451-example held-out test split.

- Total test examples: **451**
- Closed-ended (yes/no): **251** (118 "yes", 133 "no")
- Open-ended: **200**
- Question length: mean **6.88** words, median **7**, range **3–22**
- Closed-question answer length: **1** word (by construction — yes/no)
- Open-question answer length: mean **2.27** words, median **2**

## ROCOv2 (Retrieval Corpus)

Full-corpus statistics (image count by modality, caption length distribution across all ~59,962 pairs) require the raw `manifest.jsonl` produced by `scripts/download_data.py --dataset rocov2`, which is not included in this results snapshot — only retrieval *outputs* against the corpus are available here. The following is computed from the 2,255 retrieved evidence records (k=5 × 451 test queries) logged in `rag_predictions.jsonl`:

- Unique ROCOv2 pairs surfaced across all retrievals: **739** (out of ~59,962 in the corpus)
- Retrieved caption length: mean **17.41** words, range **2–98**
- Top-1 retrieval similarity: mean **0.9714**, range **0.9224–0.9902**
- All retrieved similarity scores (k=5, all ranks): mean **0.9691**, range **0.9131–0.9902**
- Test queries with zero retrieved evidence: **0 / 451 (0.0%)**

**Note:** these are evidence-side statistics only (i.e., properties of what got retrieved), not a corpus-wide EDA. A full corpus profile (modality breakdown, caption length distribution over all 59,962 pairs, embedding-space coverage) requires re-running `scripts/download_data.py --dataset rocov2` and `scripts/run_eda.py` with the ROCOv2 manifest present, since that manifest isn't part of this results snapshot.