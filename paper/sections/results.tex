% AUTO-GENERATED by scripts/generate_paper_results.py.
% Do NOT hand-edit -- re-run the script after any new evaluation run.
% Source: experiments/logs/baseline_metrics.json, rag_metrics.json,
%         baseline_predictions.jsonl, rag_predictions.jsonl, topk_sweep.json

Table~\ref{tab:headline-results} reports headline metrics for both
conditions on the VQA-RAD test split, computed by the identical
scoring code (\texttt{src/evaluation/metrics.py}) for both.

\begin{table}[t]
\centering
\caption{Baseline vs.\ retrieval-augmented results on the VQA-RAD test split.}
\label{tab:headline-results}
\begin{tabular}{@{}lccc@{}}
\toprule
Metric & Baseline & RAG & Delta \\
\midrule
Closed-question accuracy & 0.4303 & 0.4781 & +0.0478 \\
Open-question BLEU-4 & 0.0359 & 0.0389 & +0.0030 \\
Open-question ROUGE-L & 0.1422 & 0.1428 & +0.0006 \\
Overall exact match & 0.2683 & 0.2949 & +0.0266 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:significance} reports the paired bootstrap
significance test (10{,}000 resamples, 95\% confidence interval)
for each comparison, computed by
\texttt{src/evaluation/significance.py}.

\begin{table}[t]
\centering
\caption{Paired bootstrap significance test results.}
\label{tab:significance}
\begin{tabular}{@{}lcccc@{}}
\toprule
Comparison & Delta & 95\% CI & $p$ & Sig.\ ($p<0.05$) \\
\midrule
closed\_exact\_match & +0.0478 & [-0.0199, 0.1155] & 0.1848 & No \\
open\_rouge\_l & +0.0006 & [-0.0321, 0.0328] & 0.9642 & No \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:topk-sweep} reports the top-$k$ sweep from
\texttt{scripts/run\_experiments.py}.

\begin{table}[t]
\centering
\caption{Retrieval-augmented accuracy as a function of top-$k$.}
\label{tab:topk-sweep}
\begin{tabular}{@{}lccc@{}}
\toprule
$k$ & Closed Acc.\ & Open BLEU & Open ROUGE-L \\
\midrule
1 & 0.5498 & 0.0412 & 0.1492 \\
3 & 0.4900 & 0.0392 & 0.1447 \\
5 & 0.4781 & 0.0389 & 0.1428 \\
10 & 0.4661 & 0.0399 & 0.1516 \\
\bottomrule
\end{tabular}
\end{table}

Table~\ref{tab:headline-results} shows the retrieval-augmented condition
outperformed the baseline on every reported metric at $k=5$: closed-question
accuracy rose from 43.0\% to 47.8\% (+4.8 points), overall exact match from
26.8\% to 29.5\% (+2.7 points), with smaller gains on open-question BLEU-4
and ROUGE-L. Table~\ref{tab:significance} reports the paired bootstrap
significance test on both the closed-question exact-match delta and the
open-question ROUGE-L delta; neither reaches significance at $\alpha=0.05$
($p=0.1848$ and $p=0.9642$ respectively, both confidence intervals spanning
zero). The closed-question result is the more suggestive of the two --
its $p$-value is closer to conventional significance thresholds and its
confidence interval's lower bound is close to zero -- but with $n=251$
closed-question examples we cannot reject the null hypothesis that this
delta arose by chance. The open-question ROUGE-L delta is close to zero
in absolute terms and correspondingly far from significant. Table~\ref{tab:topk-sweep}
shows the same pattern observed at $k=5$ recurs across the sweep: closed
accuracy is highest at $k=1$ (54.98\%) and declines monotonically through
$k=10$ (46.61\%), a pattern discussed further in Section~\ref{sec:discussion}.